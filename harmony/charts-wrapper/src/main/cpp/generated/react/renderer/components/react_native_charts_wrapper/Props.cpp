
/**
 * This code was generated by [react-native-codegen](https://www.npmjs.com/package/react-native-codegen).
 *
 * Do not edit this file as changes may cause incorrect behavior and will be lost
 * once the code is regenerated.
 *
 * @generated by codegen project: GeneratePropsCpp.js
 */

#include "Props.h"
#include <react/renderer/core/PropsParserContext.h>
#include <react/renderer/core/propsConversions.h>

namespace facebook::react {

RNBarChartProps::RNBarChartProps(
    const PropsParserContext &context,
    const RNBarChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    drawValueAboveBar(convertRawProp(context, rawProps, "drawValueAboveBar", sourceProps.drawValueAboveBar, {false})),
    drawBarShadow(convertRawProp(context, rawProps, "drawBarShadow", sourceProps.drawBarShadow, {false})),
    highlightFullBarEnabled(convertRawProp(context, rawProps, "highlightFullBarEnabled", sourceProps.highlightFullBarEnabled, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNBarChartProps::getDiffPropsImplementationTarget() const {
  return "RNBarChart";
}

folly::dynamic RNBarChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNBarChartProps();
  const RNBarChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNBarChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (drawValueAboveBar != oldProps->drawValueAboveBar) {
    result["drawValueAboveBar"] = drawValueAboveBar;
  }
    
  if (drawBarShadow != oldProps->drawBarShadow) {
    result["drawBarShadow"] = drawBarShadow;
  }
    
  if (highlightFullBarEnabled != oldProps->highlightFullBarEnabled) {
    result["highlightFullBarEnabled"] = highlightFullBarEnabled;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNBubbleChartProps::RNBubbleChartProps(
    const PropsParserContext &context,
    const RNBubbleChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNBubbleChartProps::getDiffPropsImplementationTarget() const {
  return "RNBubbleChart";
}

folly::dynamic RNBubbleChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNBubbleChartProps();
  const RNBubbleChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNBubbleChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNCandleStickChartProps::RNCandleStickChartProps(
    const PropsParserContext &context,
    const RNCandleStickChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNCandleStickChartProps::getDiffPropsImplementationTarget() const {
  return "RNCandleStickChart";
}

folly::dynamic RNCandleStickChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNCandleStickChartProps();
  const RNCandleStickChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNCandleStickChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNCombinedChartProps::RNCombinedChartProps(
    const PropsParserContext &context,
    const RNCombinedChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    drawOrder(convertRawProp(context, rawProps, "drawOrder", sourceProps.drawOrder, {std::string{"BAR"}})),
    drawValueAboveBar(convertRawProp(context, rawProps, "drawValueAboveBar", sourceProps.drawValueAboveBar, {false})),
    highlightFullBarEnabled(convertRawProp(context, rawProps, "highlightFullBarEnabled", sourceProps.highlightFullBarEnabled, {false})),
    drawBarShadow(convertRawProp(context, rawProps, "drawBarShadow", sourceProps.drawBarShadow, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNCombinedChartProps::getDiffPropsImplementationTarget() const {
  return "RNCombinedChart";
}

folly::dynamic RNCombinedChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNCombinedChartProps();
  const RNCombinedChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNCombinedChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (drawOrder != oldProps->drawOrder) {
    result["drawOrder"] = drawOrder;
  }
    
  if (drawValueAboveBar != oldProps->drawValueAboveBar) {
    result["drawValueAboveBar"] = drawValueAboveBar;
  }
    
  if (highlightFullBarEnabled != oldProps->highlightFullBarEnabled) {
    result["highlightFullBarEnabled"] = highlightFullBarEnabled;
  }
    
  if (drawBarShadow != oldProps->drawBarShadow) {
    result["drawBarShadow"] = drawBarShadow;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNHorizontalBarChartProps::RNHorizontalBarChartProps(
    const PropsParserContext &context,
    const RNHorizontalBarChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    drawValueAboveBar(convertRawProp(context, rawProps, "drawValueAboveBar", sourceProps.drawValueAboveBar, {false})),
    drawBarShadow(convertRawProp(context, rawProps, "drawBarShadow", sourceProps.drawBarShadow, {false})),
    highlightFullBarEnabled(convertRawProp(context, rawProps, "highlightFullBarEnabled", sourceProps.highlightFullBarEnabled, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNHorizontalBarChartProps::getDiffPropsImplementationTarget() const {
  return "RNHorizontalBarChart";
}

folly::dynamic RNHorizontalBarChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNHorizontalBarChartProps();
  const RNHorizontalBarChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNHorizontalBarChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (drawValueAboveBar != oldProps->drawValueAboveBar) {
    result["drawValueAboveBar"] = drawValueAboveBar;
  }
    
  if (drawBarShadow != oldProps->drawBarShadow) {
    result["drawBarShadow"] = drawBarShadow;
  }
    
  if (highlightFullBarEnabled != oldProps->highlightFullBarEnabled) {
    result["highlightFullBarEnabled"] = highlightFullBarEnabled;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNLineChartProps::RNLineChartProps(
    const PropsParserContext &context,
    const RNLineChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNLineChartProps::getDiffPropsImplementationTarget() const {
  return "RNLineChart";
}

folly::dynamic RNLineChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNLineChartProps();
  const RNLineChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNLineChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNPieChartProps::RNPieChartProps(
    const PropsParserContext &context,
    const RNPieChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    rotationEnabled(convertRawProp(context, rawProps, "rotationEnabled", sourceProps.rotationEnabled, {false})),
    rotationAngle(convertRawProp(context, rawProps, "rotationAngle", sourceProps.rotationAngle, {0.0})),
    drawEntryLabels(convertRawProp(context, rawProps, "drawEntryLabels", sourceProps.drawEntryLabels, {false})),
    usePercentValues(convertRawProp(context, rawProps, "usePercentValues", sourceProps.usePercentValues, {false})),
    centerText(convertRawProp(context, rawProps, "centerText", sourceProps.centerText, {})),
    styledCenterText(convertRawProp(context, rawProps, "styledCenterText", sourceProps.styledCenterText, {})),
    centerTextRadiusPercent(convertRawProp(context, rawProps, "centerTextRadiusPercent", sourceProps.centerTextRadiusPercent, {0.0})),
    holeRadius(convertRawProp(context, rawProps, "holeRadius", sourceProps.holeRadius, {0.0})),
    holeColor(convertRawProp(context, rawProps, "holeColor", sourceProps.holeColor, {0.0})),
    transparentCircleRadius(convertRawProp(context, rawProps, "transparentCircleRadius", sourceProps.transparentCircleRadius, {0.0})),
    transparentCircleColor(convertRawProp(context, rawProps, "transparentCircleColor", sourceProps.transparentCircleColor, {0.0})),
    entryLabelColor(convertRawProp(context, rawProps, "entryLabelColor", sourceProps.entryLabelColor, {0.0})),
    entryLabelTextSize(convertRawProp(context, rawProps, "entryLabelTextSize", sourceProps.entryLabelTextSize, {0.0})),
    entryLabelFontFamily(convertRawProp(context, rawProps, "entryLabelFontFamily", sourceProps.entryLabelFontFamily, {std::string{"sans-serif"}})),
    maxAngle(convertRawProp(context, rawProps, "maxAngle", sourceProps.maxAngle, {0.0})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNPieChartProps::getDiffPropsImplementationTarget() const {
  return "RNPieChart";
}

folly::dynamic RNPieChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNPieChartProps();
  const RNPieChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNPieChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if (rotationEnabled != oldProps->rotationEnabled) {
    result["rotationEnabled"] = rotationEnabled;
  }
    
  if ((rotationAngle != oldProps->rotationAngle) && !(std::isnan(rotationAngle) && std::isnan(oldProps->rotationAngle))) {
    result["rotationAngle"] = rotationAngle;
  }
    
  if (drawEntryLabels != oldProps->drawEntryLabels) {
    result["drawEntryLabels"] = drawEntryLabels;
  }
    
  if (usePercentValues != oldProps->usePercentValues) {
    result["usePercentValues"] = usePercentValues;
  }
    
  if (centerText != oldProps->centerText) {
    result["centerText"] = centerText;
  }
    
  if (styledCenterText != oldProps->styledCenterText) {
    result["styledCenterText"] = toDynamic(styledCenterText);
  }
    
  if ((centerTextRadiusPercent != oldProps->centerTextRadiusPercent) && !(std::isnan(centerTextRadiusPercent) && std::isnan(oldProps->centerTextRadiusPercent))) {
    result["centerTextRadiusPercent"] = centerTextRadiusPercent;
  }
    
  if ((holeRadius != oldProps->holeRadius) && !(std::isnan(holeRadius) && std::isnan(oldProps->holeRadius))) {
    result["holeRadius"] = holeRadius;
  }
    
  if ((holeColor != oldProps->holeColor) && !(std::isnan(holeColor) && std::isnan(oldProps->holeColor))) {
    result["holeColor"] = holeColor;
  }
    
  if ((transparentCircleRadius != oldProps->transparentCircleRadius) && !(std::isnan(transparentCircleRadius) && std::isnan(oldProps->transparentCircleRadius))) {
    result["transparentCircleRadius"] = transparentCircleRadius;
  }
    
  if ((transparentCircleColor != oldProps->transparentCircleColor) && !(std::isnan(transparentCircleColor) && std::isnan(oldProps->transparentCircleColor))) {
    result["transparentCircleColor"] = transparentCircleColor;
  }
    
  if ((entryLabelColor != oldProps->entryLabelColor) && !(std::isnan(entryLabelColor) && std::isnan(oldProps->entryLabelColor))) {
    result["entryLabelColor"] = entryLabelColor;
  }
    
  if ((entryLabelTextSize != oldProps->entryLabelTextSize) && !(std::isnan(entryLabelTextSize) && std::isnan(oldProps->entryLabelTextSize))) {
    result["entryLabelTextSize"] = entryLabelTextSize;
  }
    
  if (entryLabelFontFamily != oldProps->entryLabelFontFamily) {
    result["entryLabelFontFamily"] = entryLabelFontFamily;
  }
    
  if ((maxAngle != oldProps->maxAngle) && !(std::isnan(maxAngle) && std::isnan(oldProps->maxAngle))) {
    result["maxAngle"] = maxAngle;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNRadarChartProps::RNRadarChartProps(
    const PropsParserContext &context,
    const RNRadarChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    rotationEnabled(convertRawProp(context, rawProps, "rotationEnabled", sourceProps.rotationEnabled, {false})),
    rotationAngle(convertRawProp(context, rawProps, "rotationAngle", sourceProps.rotationAngle, {0.0})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    drawWeb(convertRawProp(context, rawProps, "drawWeb", sourceProps.drawWeb, {false})),
    skipWebLineCount(convertRawProp(context, rawProps, "skipWebLineCount", sourceProps.skipWebLineCount, {0.0})),
    webLineWidth(convertRawProp(context, rawProps, "webLineWidth", sourceProps.webLineWidth, {0.0})),
    webLineWidthInner(convertRawProp(context, rawProps, "webLineWidthInner", sourceProps.webLineWidthInner, {0.0})),
    webAlpha(convertRawProp(context, rawProps, "webAlpha", sourceProps.webAlpha, {0.0})),
    webColor(convertRawProp(context, rawProps, "webColor", sourceProps.webColor, {0.0})),
    webColorInner(convertRawProp(context, rawProps, "webColorInner", sourceProps.webColorInner, {0.0})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNRadarChartProps::getDiffPropsImplementationTarget() const {
  return "RNRadarChart";
}

folly::dynamic RNRadarChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNRadarChartProps();
  const RNRadarChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNRadarChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if (rotationEnabled != oldProps->rotationEnabled) {
    result["rotationEnabled"] = rotationEnabled;
  }
    
  if ((rotationAngle != oldProps->rotationAngle) && !(std::isnan(rotationAngle) && std::isnan(oldProps->rotationAngle))) {
    result["rotationAngle"] = rotationAngle;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (drawWeb != oldProps->drawWeb) {
    result["drawWeb"] = drawWeb;
  }
    
  if ((skipWebLineCount != oldProps->skipWebLineCount) && !(std::isnan(skipWebLineCount) && std::isnan(oldProps->skipWebLineCount))) {
    result["skipWebLineCount"] = skipWebLineCount;
  }
    
  if ((webLineWidth != oldProps->webLineWidth) && !(std::isnan(webLineWidth) && std::isnan(oldProps->webLineWidth))) {
    result["webLineWidth"] = webLineWidth;
  }
    
  if ((webLineWidthInner != oldProps->webLineWidthInner) && !(std::isnan(webLineWidthInner) && std::isnan(oldProps->webLineWidthInner))) {
    result["webLineWidthInner"] = webLineWidthInner;
  }
    
  if ((webAlpha != oldProps->webAlpha) && !(std::isnan(webAlpha) && std::isnan(oldProps->webAlpha))) {
    result["webAlpha"] = webAlpha;
  }
    
  if ((webColor != oldProps->webColor) && !(std::isnan(webColor) && std::isnan(oldProps->webColor))) {
    result["webColor"] = webColor;
  }
    
  if ((webColorInner != oldProps->webColorInner) && !(std::isnan(webColorInner) && std::isnan(oldProps->webColorInner))) {
    result["webColorInner"] = webColorInner;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif
RNScatterChartProps::RNScatterChartProps(
    const PropsParserContext &context,
    const RNScatterChartProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    animation(convertRawProp(context, rawProps, "animation", sourceProps.animation, {})),
    chartBackgroundColor(convertRawProp(context, rawProps, "chartBackgroundColor", sourceProps.chartBackgroundColor, {0.0})),
    logEnabled(convertRawProp(context, rawProps, "logEnabled", sourceProps.logEnabled, {false})),
    noDataText(convertRawProp(context, rawProps, "noDataText", sourceProps.noDataText, {})),
    noDataTextColor(convertRawProp(context, rawProps, "noDataTextColor", sourceProps.noDataTextColor, {0.0})),
    touchEnabled(convertRawProp(context, rawProps, "touchEnabled", sourceProps.touchEnabled, {false})),
    dragDecelerationEnabled(convertRawProp(context, rawProps, "dragDecelerationEnabled", sourceProps.dragDecelerationEnabled, {false})),
    dragDecelerationFrictionCoef(convertRawProp(context, rawProps, "dragDecelerationFrictionCoef", sourceProps.dragDecelerationFrictionCoef, {0.0})),
    highlightPerTapEnabled(convertRawProp(context, rawProps, "highlightPerTapEnabled", sourceProps.highlightPerTapEnabled, {false})),
    chartDescription(convertRawProp(context, rawProps, "chartDescription", sourceProps.chartDescription, {})),
    legend(convertRawProp(context, rawProps, "legend", sourceProps.legend, {})),
    xAxis(convertRawProp(context, rawProps, "xAxis", sourceProps.xAxis, {})),
    marker(convertRawProp(context, rawProps, "marker", sourceProps.marker, {})),
    highlights(convertRawProp(context, rawProps, "highlights", sourceProps.highlights, {})),
    maxHighlightDistance(convertRawProp(context, rawProps, "maxHighlightDistance", sourceProps.maxHighlightDistance, {0.0})),
    drawGridBackground(convertRawProp(context, rawProps, "drawGridBackground", sourceProps.drawGridBackground, {false})),
    gridBackgroundColor(convertRawProp(context, rawProps, "gridBackgroundColor", sourceProps.gridBackgroundColor, {0.0})),
    drawBorders(convertRawProp(context, rawProps, "drawBorders", sourceProps.drawBorders, {false})),
    borderColor(convertRawProp(context, rawProps, "borderColor", sourceProps.borderColor, {0.0})),
    borderWidth(convertRawProp(context, rawProps, "borderWidth", sourceProps.borderWidth, {0.0})),
    minOffset(convertRawProp(context, rawProps, "minOffset", sourceProps.minOffset, {0.0})),
    maxVisibleValueCount(convertRawProp(context, rawProps, "maxVisibleValueCount", sourceProps.maxVisibleValueCount, {0.0})),
    visibleRange(convertRawProp(context, rawProps, "visibleRange", sourceProps.visibleRange, {})),
    autoScaleMinMaxEnabled(convertRawProp(context, rawProps, "autoScaleMinMaxEnabled", sourceProps.autoScaleMinMaxEnabled, {false})),
    keepPositionOnRotation(convertRawProp(context, rawProps, "keepPositionOnRotation", sourceProps.keepPositionOnRotation, {false})),
    highlightPerDragEnabled(convertRawProp(context, rawProps, "highlightPerDragEnabled", sourceProps.highlightPerDragEnabled, {false})),
    scaleEnabled(convertRawProp(context, rawProps, "scaleEnabled", sourceProps.scaleEnabled, {false})),
    scaleXEnabled(convertRawProp(context, rawProps, "scaleXEnabled", sourceProps.scaleXEnabled, {false})),
    scaleYEnabled(convertRawProp(context, rawProps, "scaleYEnabled", sourceProps.scaleYEnabled, {false})),
    dragEnabled(convertRawProp(context, rawProps, "dragEnabled", sourceProps.dragEnabled, {false})),
    pinchZoom(convertRawProp(context, rawProps, "pinchZoom", sourceProps.pinchZoom, {false})),
    doubleTapToZoomEnabled(convertRawProp(context, rawProps, "doubleTapToZoomEnabled", sourceProps.doubleTapToZoomEnabled, {false})),
    yAxis(convertRawProp(context, rawProps, "yAxis", sourceProps.yAxis, {})),
    zoom(convertRawProp(context, rawProps, "zoom", sourceProps.zoom, {})),
    viewPortOffsets(convertRawProp(context, rawProps, "viewPortOffsets", sourceProps.viewPortOffsets, {})),
    extraOffsets(convertRawProp(context, rawProps, "extraOffsets", sourceProps.extraOffsets, {})),
    group(convertRawProp(context, rawProps, "group", sourceProps.group, {})),
    identifier(convertRawProp(context, rawProps, "identifier", sourceProps.identifier, {})),
    syncX(convertRawProp(context, rawProps, "syncX", sourceProps.syncX, {false})),
    syncY(convertRawProp(context, rawProps, "syncY", sourceProps.syncY, {false})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName RNScatterChartProps::getDiffPropsImplementationTarget() const {
  return "RNScatterChart";
}

folly::dynamic RNScatterChartProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = RNScatterChartProps();
  const RNScatterChartProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const RNScatterChartProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (animation != oldProps->animation) {
    result["animation"] = toDynamic(animation);
  }
    
  if ((chartBackgroundColor != oldProps->chartBackgroundColor) && !(std::isnan(chartBackgroundColor) && std::isnan(oldProps->chartBackgroundColor))) {
    result["chartBackgroundColor"] = chartBackgroundColor;
  }
    
  if (logEnabled != oldProps->logEnabled) {
    result["logEnabled"] = logEnabled;
  }
    
  if (noDataText != oldProps->noDataText) {
    result["noDataText"] = noDataText;
  }
    
  if ((noDataTextColor != oldProps->noDataTextColor) && !(std::isnan(noDataTextColor) && std::isnan(oldProps->noDataTextColor))) {
    result["noDataTextColor"] = noDataTextColor;
  }
    
  if (touchEnabled != oldProps->touchEnabled) {
    result["touchEnabled"] = touchEnabled;
  }
    
  if (dragDecelerationEnabled != oldProps->dragDecelerationEnabled) {
    result["dragDecelerationEnabled"] = dragDecelerationEnabled;
  }
    
  if ((dragDecelerationFrictionCoef != oldProps->dragDecelerationFrictionCoef) && !(std::isnan(dragDecelerationFrictionCoef) && std::isnan(oldProps->dragDecelerationFrictionCoef))) {
    result["dragDecelerationFrictionCoef"] = dragDecelerationFrictionCoef;
  }
    
  if (highlightPerTapEnabled != oldProps->highlightPerTapEnabled) {
    result["highlightPerTapEnabled"] = highlightPerTapEnabled;
  }
    
  if (chartDescription != oldProps->chartDescription) {
    result["chartDescription"] = toDynamic(chartDescription);
  }
    
  if (legend != oldProps->legend) {
    result["legend"] = toDynamic(legend);
  }
    
  if (xAxis != oldProps->xAxis) {
    result["xAxis"] = toDynamic(xAxis);
  }
    
  if (marker != oldProps->marker) {
    result["marker"] = toDynamic(marker);
  }
    
  if (highlights != oldProps->highlights) {
    result["highlights"] = toDynamic(highlights);
  }
    
  if ((maxHighlightDistance != oldProps->maxHighlightDistance) && !(std::isnan(maxHighlightDistance) && std::isnan(oldProps->maxHighlightDistance))) {
    result["maxHighlightDistance"] = maxHighlightDistance;
  }
    
  if (drawGridBackground != oldProps->drawGridBackground) {
    result["drawGridBackground"] = drawGridBackground;
  }
    
  if ((gridBackgroundColor != oldProps->gridBackgroundColor) && !(std::isnan(gridBackgroundColor) && std::isnan(oldProps->gridBackgroundColor))) {
    result["gridBackgroundColor"] = gridBackgroundColor;
  }
    
  if (drawBorders != oldProps->drawBorders) {
    result["drawBorders"] = drawBorders;
  }
    
  if ((borderColor != oldProps->borderColor) && !(std::isnan(borderColor) && std::isnan(oldProps->borderColor))) {
    result["borderColor"] = borderColor;
  }
    
  if ((borderWidth != oldProps->borderWidth) && !(std::isnan(borderWidth) && std::isnan(oldProps->borderWidth))) {
    result["borderWidth"] = borderWidth;
  }
    
  if ((minOffset != oldProps->minOffset) && !(std::isnan(minOffset) && std::isnan(oldProps->minOffset))) {
    result["minOffset"] = minOffset;
  }
    
  if ((maxVisibleValueCount != oldProps->maxVisibleValueCount) && !(std::isnan(maxVisibleValueCount) && std::isnan(oldProps->maxVisibleValueCount))) {
    result["maxVisibleValueCount"] = maxVisibleValueCount;
  }
    
  if (visibleRange != oldProps->visibleRange) {
    result["visibleRange"] = toDynamic(visibleRange);
  }
    
  if (autoScaleMinMaxEnabled != oldProps->autoScaleMinMaxEnabled) {
    result["autoScaleMinMaxEnabled"] = autoScaleMinMaxEnabled;
  }
    
  if (keepPositionOnRotation != oldProps->keepPositionOnRotation) {
    result["keepPositionOnRotation"] = keepPositionOnRotation;
  }
    
  if (highlightPerDragEnabled != oldProps->highlightPerDragEnabled) {
    result["highlightPerDragEnabled"] = highlightPerDragEnabled;
  }
    
  if (scaleEnabled != oldProps->scaleEnabled) {
    result["scaleEnabled"] = scaleEnabled;
  }
    
  if (scaleXEnabled != oldProps->scaleXEnabled) {
    result["scaleXEnabled"] = scaleXEnabled;
  }
    
  if (scaleYEnabled != oldProps->scaleYEnabled) {
    result["scaleYEnabled"] = scaleYEnabled;
  }
    
  if (dragEnabled != oldProps->dragEnabled) {
    result["dragEnabled"] = dragEnabled;
  }
    
  if (pinchZoom != oldProps->pinchZoom) {
    result["pinchZoom"] = pinchZoom;
  }
    
  if (doubleTapToZoomEnabled != oldProps->doubleTapToZoomEnabled) {
    result["doubleTapToZoomEnabled"] = doubleTapToZoomEnabled;
  }
    
  if (yAxis != oldProps->yAxis) {
    result["yAxis"] = toDynamic(yAxis);
  }
    
  if (zoom != oldProps->zoom) {
    result["zoom"] = toDynamic(zoom);
  }
    
  if (viewPortOffsets != oldProps->viewPortOffsets) {
    result["viewPortOffsets"] = toDynamic(viewPortOffsets);
  }
    
  if (extraOffsets != oldProps->extraOffsets) {
    result["extraOffsets"] = toDynamic(extraOffsets);
  }
    
  if (group != oldProps->group) {
    result["group"] = group;
  }
    
  if (identifier != oldProps->identifier) {
    result["identifier"] = identifier;
  }
    
  if (syncX != oldProps->syncX) {
    result["syncX"] = syncX;
  }
    
  if (syncY != oldProps->syncY) {
    result["syncY"] = syncY;
  }
    
  if (data != oldProps->data) {
    result["data"] = toDynamic(data);
  }
  return result;
}
#endif

} // namespace facebook::react
